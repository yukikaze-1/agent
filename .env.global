# Agent 全局环境配置
# Project:      Agent
# Author:       yomu
# Time:         2025/01/06
# Version:      2.0
# Description:  Agent 系统全局环境变量配置

# === 基础路径配置 ===
# 注意：AGENT_HOME 和 PYTHONPATH 将由 agent_v0.1.py 自动设置为当前项目路径
# 这里设置为占位符，实际运行时会被替换
AGENT_HOME=${AGENT_HOME}
PYTHONPATH=${PYTHONPATH}

# === 环境级别 ===
AGENT_ENV=development

# === 日志配置 ===
LOG_LEVEL=INFO
LOG_PATH=${AGENT_HOME}/Log

# === 服务配置 ===
ENABLE_EXTERNAL_SERVICES=true
ENABLE_INTERNAL_MODULES=true
ENABLE_HEALTH_CHECK=true

# === 网络配置 ===
DEFAULT_HOST=localhost
DEFAULT_PORT_RANGE_START=8000
DEFAULT_PORT_RANGE_END=9000

# === 数据库配置 ===
DB_HOST=localhost
DB_PORT=3306
DB_NAME=agent_db

# === 缓存配置 ===
REDIS_HOST=localhost
REDIS_PORT=6379

# === AI 模型配置 ===
MODEL_CACHE_PATH=${AGENT_HOME}/Data/models
TEMP_PATH=${AGENT_HOME}/Temp

# === 安全配置 ===
API_KEY_FILE=${AGENT_HOME}/Config/api_keys.env
SECRET_KEY_FILE=${AGENT_HOME}/Config/secrets.env
